[package]
name = "site-generate"
version = "0.1.0"
edition = "2021"

# See more keys and their definitions at https://doc.rust-lang.org/cargo/reference/manifest.html

[dependencies]
anyhow = "1.0.71"
wiki = { path = "../wiki", features = ["dom-ssr"] }
urlencoding = "2.1.2"
